language: python

python:
  - '3.8'

install:
  - pip install -r requirements.txt

script:
  - echo "Ready to deploy"

deploy:
  provider: gae
  keyfile: myhelsinki-aaf81aa8a071.json
  project: myhelsinki
  edge: true

before_install:
  - openssl aes-256-cbc -K $encrypted_286fbecaf8cc_key -iv $encrypted_286fbecaf8cc_iv -in myhelsinki-aaf81aa8a071.json.enc -out myhelsinki-aaf81aa8a071.json -d
